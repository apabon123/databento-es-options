# FRED API configuration.
# Prefer setting FRED_API_KEY in .env at project root (not committed).
# Optional fallback: set api_key here (e.g. for CI); get a key at https://fred.stlouisfed.org/docs/api/api_key.html
# api_key: "your_fred_api_key_here"
default_start: 1990-01-01
default_end: null  # null => use today's date

